# IServerCore

Paper plugin core for server attributes, YAML utilities, and task management.

## Repository Status

- Visibility: public
- Recommendation: merge-refactor
- Publish assessment: 不建议单独发布。
- Reviewed: 2026-07-05

## Value

服务器属性核心，与 RPGServerCore 有重叠。

## Future Value

低到中；应统一到一个核心。

## Maintenance Notes

与 RPGServerCore 合并/取舍。

## Project Shape

- Main language: Java
- File count: 27
- Detected manifests: build.gradle, settings.gradle
- Top-level entries: .gitignore, build.gradle, gradle, gradle.properties, gradlew, gradlew.bat, settings.gradle, src

## Build / Run Notes

- Gradle project: run ./gradlew build or gradlew.bat build depending on platform.
